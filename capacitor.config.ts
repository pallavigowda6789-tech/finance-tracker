import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.madproject.financetracker',
  appName: 'FinTrack',
  webDir: 'build'
};

export default config;
